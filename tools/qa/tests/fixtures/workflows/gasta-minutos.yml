# Fixture: workflow que consumiria minutos de GitHub.
# Es el error que el chequeo tiene que cazar: alcanza con una linea para que el
# equipo se quede sin CI a mitad de mes.
name: fixture
on: [push]
jobs:
  malo:
    runs-on: ubuntu-latest
    steps:
      - run: echo "esto correria en la infraestructura de GitHub"
  bueno:
    runs-on: self-hosted
    steps:
      - run: echo "esto corre en el server del equipo, gratis"
