# Rutas que son nuestras. Lo que caiga fuera de aca se reporta como informativo
# pero NUNCA bloquea, aunque lo toque un merge.
#
# Un glob por linea. Las lineas que empiezan con # son comentarios.
# Cuando entre contenido de otro equipo al repo, NO lo agregues aca.

docs/**
*.md
codigo-ejemplo/ms-evaluacion-llm/**
tools/qa/**
tools/ci-front/**

# Nuestro workflow, por nombre y no por carpeta. `.github/workflows/` vive en la
# raiz del repositorio y no se puede prefijar: en el monorepo va a ser una sola
# carpeta compartida por todos los equipos. Por eso la etapa `workflows` revisa
# solo los archivos que matchean aca, y en la mudanza esta linea pasa a ser algo
# como `.github/workflows/tema-07-*.yml`, con el workflow renombrado igual.
.github/workflows/qa.yml

# Exclusiones (la exclusion siempre gana). Los fixtures del self-test estan
# rotos a proposito: si el gate se chequeara a si mismo con ellos, fallaria
# siempre y por las razones equivocadas.
!tools/qa/tests/fixtures/**

# Material importado de otras ramas. Vive adentro de `docs/` para que la raiz
# tenga una sola carpeta de documentacion, pero NO es nuestro: se importo sin
# tocar una linea. Sin esta exclusion, `docs/**` lo volveria propio y el gate
# empezaria a bloquear por texto que no escribimos ni podemos reescribir.
!docs/importado/**
