# La carpeta del equipo DENTRO del repositorio. Una sola linea, sin barras al
# principio ni al final. Vacio significa "somos la raiz", que es el caso de hoy.
#
# Existe para la mudanza al monorepo de la materia. Ahi todo esto pasa a colgar
# de una carpeta del equipo, y sin este valor habria que reescribir a mano los
# globs de owned-paths.txt, la ruta del proyecto Java en orquestar.py y las
# raices de check_refs.py. Con este valor, mudarse es cambiar esta linea.
#
# Ejemplo, cuando se sepa el nombre:
#   tema-07
#
# Se puede pisar por entorno con QA_PREFIJO, que sirve para probar el cambio sin
# commitearlo. NO lo uses como forma normal de configurarlo: si el valor vive en
# la variable de entorno de cada persona, al que se olvide de setearla el gate le
# deja TODO fuera de alcance y la corrida termina en verde sin haber mirado un
# archivo. Por eso el valor de verdad vive aca, versionado, y es igual para todos.
#
# Si apunta a una carpeta que no existe, el gate falla y lo dice. Es a proposito:
# el modo de falla que hay que evitar es el silencioso.
