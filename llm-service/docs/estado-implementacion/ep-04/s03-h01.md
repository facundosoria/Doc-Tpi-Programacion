# LLM-S03-H01 — Correr y activar una calibración de curso con métrica PAR-14

- **Estado:** 🔴 Hueco crítico — ningún run puede terminar
- **Ficha (con estado embebido):** [`docs/historias/ep-04/s03-h01.md`](../../historias/ep-04/s03-h01.md)
- **Código:** `CalibrationRunController`, `CalibrationActivationController`,
  `CalibrationWorkflowService`, `CalibrationMetrics`
- **Evidencia:** [`verificacion-v2-golden-set-calibracion.md` · §3](../../entregas/verificacion-v2-golden-set-calibracion.md),
  [`CORRECCIONES-SUGERIDAS.md` ítem 2](../../../CORRECCIONES-SUGERIDAS.md)

## Resumen

- Encolar (`QUEUED`), reclamar (`@Scheduled` cada 1s) y pasar a `RUNNING`: **funciona**, bien
  probado (`CalibrationMetricsTest`, `CalibrationStateMachineTest`).
- Activación en dos pasos (`activate-preview` con token de confirmación, `activate`): **funciona**.
- Una calibración activa por curso, estado consultable por otros servicios
  (`active-calibration`, `pending-evaluations`): **funciona**.
- **El hueco:** ningún código invoca a un modelo, real ni simulado. `CalibrationRunWorker`
  solo transiciona `QUEUED → RUNNING` y ahí se detiene — nada llama a
  `CalibrationMetrics.assess` con puntajes reales, nada llena `calibration_case_results`. **Todo
  run queda `RUNNING` para siempre.**
- CA3, CA4 y CA5 de la ficha (transición a `PASSED`/`FAILED` según el desvío PAR-14) **no se
  pueden demostrar hoy**.
- **Causa raíz exacta:** [`LLM-S01-H10`](../ep-02/h10.md) está en 0 % — sin el puerto de
  invocación de modelos, no hay dónde enchufar ni un fake.
- **Diferencia de alcance con la épica:** EP-04 describe calibración de **plataforma** (ADMIN) y
  de **curso** (docente); el código solo implementa la segunda. Ver la nota en la propia ficha.

## Para cerrar esto

1. Cerrar [`ep-02/h10.md`](../ep-02/h10.md) (el puerto + fake).
2. Agregar la pieza que hoy no tiene ficha: algo (¿extensión de `CalibrationWorkflowService` o
   un `CalibrationRunner` nuevo?) que tome el puerto de H10, corra los casos del golden set
   contra el fake, llene `calibration_case_results` y cierre el run con `CalibrationMetrics.assess`.
