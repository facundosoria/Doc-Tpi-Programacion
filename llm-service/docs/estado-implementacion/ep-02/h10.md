# LLM-S01-H10 — Puerto del proveedor de modelos (AI Gateway) y fake para pruebas

- **Estado:** 🟢 Construida — cerrada el 2026-09-12
- **Ficha:** [`docs/historias/ep-02/h10.md`](../../historias/ep-02/h10.md)
- **Evidencia:** [`CORRECCIONES-SUGERIDAS.md` ítems 2 y 21](../../../CORRECCIONES-SUGERIDAS.md)
  (ahora marcados resueltos), [`codigo-ejemplo/ms-evaluacion-llm.md`](../codigo-ejemplo/ms-evaluacion-llm.md)

## Qué se agregó

Portado (adaptado, no copiado literal) de `codigo-ejemplo/ms-evaluacion-llm`
(`LlmGateway`/`LlmGatewayImpl`/`GroqAdapter`), luego eliminada esa carpeta — ver
[decisión de consolidación](../codigo-ejemplo/README.md).

- `domain/ai/`: `ModelFunction`, `ModelInvocationRequest`/`Result`, `ModelInvocationPort`,
  `ModelResponseSchema` (+ `InvalidModelResponseException`, `ModelTimeoutException`) — java puro,
  sin Spring (regla de doc 37 §1).
- `infrastructure/ai/FakeModelAdapter` — el adaptador fake de T3; nunca llama a un proveedor real
  (a diferencia de `GroqAdapter`, acá no hay rama "real" que activar con una API key).
- `infrastructure/persistence/FunctionModelConfigRepository` + migración `V13` (tabla
  `function_model_config`, T5) — semilla `('tutor','fake','fake-socratic-v1')`.
- `application/ModelInvocationService` — resuelve la función, invoca el puerto con timeout
  (`java.util.concurrent.CompletableFuture` + `Executors.newVirtualThreadPerTaskExecutor()`),
  valida el schema.
- `api/ModelAssignmentController` — `GET`/`PUT /api/llm/model-assignments/{function}`, tal cual
  `docs/contracts/llm-service-v1.openapi.yaml`.

## Veredicto por tarea

| Tarea | Pide | Estado |
|---|---|---|
| T1 | Interfaz de puerto en `domain`/`application` | 🟢 `ModelInvocationPort` |
| T2 | Schema estricto de salida + validador | 🟢 `ModelResponseSchema` (hoy solo sabe validar `TUTOR`) |
| T3 | Adaptador fake | 🟢 `FakeModelAdapter` — sin WireMock, simulado en proceso (más simple, mismo resultado) |
| T4 | Timeout configurable + error controlado | 🟢 `ModelTimeoutException`, cortado vía `CompletableFuture.get(timeout)` |
| T5 | Migración de tabla `función → proveedor + modelo` | 🟢 `V13__ai_gateway_and_tutor.sql` + `FunctionModelConfigRepository` |
| T6 | Pruebas de los 3 escenarios BDD | 🟢 `FakeModelAdapterTest`, `ModelInvocationServiceTest` |

**Consecuencia:** el consumidor real de este puerto hoy es
[`ep-05/interactions.md`](../ep-05/interactions.md) (el tutor). La calibración de EP-04
**todavía no está conectada** a este puerto — ver [`ep-04/s03-h01.md`](../ep-04/s03-h01.md), que
sigue con el hueco crítico: falta el paso que tome este puerto y corra los casos del golden set
contra el fake.
