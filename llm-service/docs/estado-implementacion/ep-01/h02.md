# LLM-S01-H02 — Entorno reproducible con un comando

- **Estado:** 🟡 Parcial
- **Ficha:** [`docs/historias/ep-01/h02.md`](../../historias/ep-01/h02.md)
- **Evidencia:** [`CORRECCIONES-SUGERIDAS.md` ítem 19](../../../CORRECCIONES-SUGERIDAS.md)

## Qué hay en el código

- `llm-service/compose.yaml` (backend aislado) y `llm-service/compose.workbench.yaml` (demo con
  `llm-workbench` en `:4200`) existen y arrancan con `docker compose up --build`
  ([`llm-service/README.md`](../../../README.md)).
- El README documenta **dos** variantes de `up`, pero **no documenta `docker compose down`** ni
  cómo comprobar el *health* desde afuera del contenedor (solo menciona el endpoint interno
  `http://llm-service:8080/actuator/health`).
- **No existe ningún archivo `.env*`** en el repo (ni `.env.example`) — la ficha pide
  explícitamente uno con las variables del ADR (que tampoco existe, ver [H01](h01.md)).

## Veredicto por CA

| CA | Estado |
|---|---|
| CA1 — `docker compose up` deja todo *healthy* | 🟡 no verificado en esta pasada (no se ejecutó), pero el archivo está completo y con healthchecks declarados |
| CA2 — README documenta `up`, *health*, `down` | ❌ falta `down` y la verificación de *health* desde afuera |
| CA3 — sin Docker, falla con mensaje claro | ⚪ no auditado (depende del comportamiento de `docker compose`, no de código propio) |
| CA4 — puerto ocupado, error identifica el puerto | ⚪ no auditado |

**Nota:** hay un `.env.example` a nivel `llm-service/` en el `git status` del repo (`??
llm-service/.env.example`, sin trackear todavía) — confirmar si ese archivo, una vez agregado,
cierra el hueco de `.env.example` que señala este ítem.
