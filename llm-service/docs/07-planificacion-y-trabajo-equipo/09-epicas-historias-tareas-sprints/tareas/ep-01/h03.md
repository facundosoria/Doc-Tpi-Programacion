# Tareas SMART — LLM-EP01-H03 (LLM-S01-H03) · Esqueleto transversal del servicio

> **Historia padre:** [`../../historias/ep-01/h03.md`](../../historias/ep-01/h03.md) · Pareja
> P1 · 34 h. Orden: [playbook · §4](../../../06-playbook-de-construccion.md). Formato del
> [template de Tarea de Taiga](../../../10-plantillas/tarea-taiga.md); método **SMART**
> ([`../README.md`](../README.md)).

### T1 — Contrato y amenaza de los headers de borde

- **Específica:** documentar los headers de borde (`X-Service-Id`, `X-Service-Scopes`,
  `X-Delegated-User`, `X-Request-Id`, `traceparent`), el actor M2M y los modos de falla.
- **Medible:** existe la especificación de headers y amenazas revisada con D01.
- **Alcanzable:** 4 h, una persona de P1.
- **Relevante:** fija el borde antes de programarlo.
- **Acotada:** 4 h · paso *contrato y amenaza*. **Depende de:** — · **Traza:** CA2, CA4, CA5 de H03.
- **Estado (2026-09-16):** cerrada. Cubierta por la documentación técnica de plataforma existente
  ([`04-seguridad-y-pipeline-de-filtros.md`](../../../../02-arquitectura-y-plataforma/06-gateway-y-discovery/04-seguridad-y-pipeline-de-filtros.md)
  y [`05-comunicacion-micro-a-micro.md`](../../../../02-arquitectura-y-plataforma/06-gateway-y-discovery/05-comunicacion-micro-a-micro.md))
  y formalizada en las clases de autorización M2M del servicio.

### T2 — Estructura de paquetes + config base y Actuator

- **Específica:** crear la estructura de paquetes del ADR, la configuración base de Spring
  Boot 3 y el `health` de Actuator que no llama a servicios externos.
- **Medible:** `GET /actuator/health` responde sin dependencias de red externas.
- **Alcanzable:** 4 h, una persona de P1.
- **Relevante:** es el esqueleto sobre el que se montan seguridad y adaptadores.
- **Acotada:** 4 h · paso *adaptadores*. **Depende de:** H01·T2 · **Traza:** CA1 de H03.
- **Estado (2026-09-16):** cerrada. Estructura de paquetes formalizada en ADR-019 y validada por
  `ArchitectureTest`; configuración base y Actuator (`/actuator/health`, `/actuator/info`) operativos
  sin dependencias de red externas.

### T3 — Registro en Eureka y ruta por el Gateway

- **Específica:** registrar el servicio en Eureka como `llm-service` y publicar la ruta
  `/api/llm/**` por el Gateway sin reescritura de path.
- **Medible:** el servicio aparece en Eureka y responde por el Gateway en `/api/llm/**`.
- **Alcanzable:** 5 h, una persona de P1 (con D01) — cliente Eureka configurado en `application.yml`
  (`register-with-eureka: true`, `fetch-registry: false`, variable `EUREKA_URL`). No se agrega
  Eureka local a `compose.yaml` (alineado con la decisión de H02 y
  [`05-servicios-docker.md`](../../../../02-arquitectura-y-plataforma/05-servicios-docker.md) §3);
  la verificación final contra el Gateway y Eureka compartidos queda sujeta a la disponibilidad de
  dicha infraestructura compartida.
- **Relevante:** es el borde estable que consumen los demás equipos.
- **Acotada:** 5 h · paso *adaptadores*. **Depende de:** T2, H02·T2 · **Traza:** CA1 de H03.
- **Estado (2026-09-16):** sustancialmente cerrada en el servicio. Bloque `eureka:` configurado en
  `application.yml`; todas las rutas del backend responden bajo `/api/llm/**` (verificado en los 13
  controladores); verificación end-to-end pendiente de la infraestructura compartida de la plataforma.

### T4 — Seguridad M2M: audiencia y scope

- **Específica:** validar `aud=llm-service` y el scope requerido; un token sin scope devuelve
  `401` como *Problem Details*.
- **Medible:** token sin scope → `401` con cuerpo *Problem Details*.
- **Alcanzable:** 6 h, una persona de P1.
- **Relevante:** primer criterio negativo de seguridad.
- **Acotada:** 6 h · paso *seguridad y resiliencia*. **Depende de:** T1, T3 · **Traza:** CA4 de H03.
- **Estado (2026-09-16):** cerrada. Se implementó la separación 401 vs 403 en `GoldenSetAuthorization`,
  `TutorGatewayAuthorization` y `RagGatewayAuthorization`: token sin scope requerido o servicio no
  confiable responde `401 Unauthorized` ("Falta el permiso requerido") con Problem Details RFC 7807.
  Verificado con pruebas unitarias exhaustivas en cada clase.

### T5 — Identidad delegada: rechazo de `X-Delegated-User` falsificado

- **Específica:** rechazar con `403` una identidad delegada ausente o falsificada, sin
  ejecutar ninguna acción.
- **Medible:** un `X-Delegated-User` que no vino del Gateway de confianza → `403` y sin efecto.
- **Alcanzable:** 4 h, una persona de P1.
- **Relevante:** segundo criterio negativo de seguridad.
- **Acotada:** 4 h · paso *seguridad y resiliencia*. **Depende de:** T1, T4 · **Traza:** CA5 de H03.
- **Estado (2026-09-16):** cerrada. Se validó en las 3 clases de autorización que `X-Delegated-User`
  ausente ("Identidad delegada ausente") o malformado/no-UUID ("Identidad delegada inválida") responde
  `403 Forbidden` con Problem Details sin ejecutar ninguna acción de negocio. Verificado con pruebas
  unitarias en cada clase.

### T6 — Manejador global *Problem Details*

- **Específica:** implementar el manejador global RFC 7807 con `type`/`title`/`status`/`detail`
  y asegurar que los logs no registran tokens.
- **Medible:** todo error del servicio se serializa como *Problem Details*; ningún log trae tokens.
- **Alcanzable:** 4 h, una persona de P1.
- **Relevante:** formato de error uniforme que consumen los demás equipos.
- **Acotada:** 4 h · paso *observabilidad*. **Depende de:** T2 · **Traza:** CA3 de H03.
- **Estado (2026-09-16):** cerrada. `ApiExceptionHandler` incorpora
  `@ExceptionHandler(ResponseStatusException.class)` mapeando a `ProblemDetail` RFC 7807 preservando
  status, reason y correlación `requestId`. Verificado en logs que no se registran tokens sensibles.

### T7 — Correlación `traceparent` / `X-Request-Id`

- **Específica:** propagar `traceparent` y devolver el mismo `X-Request-Id` que llegó en cada
  respuesta.
- **Medible:** una request con `X-Request-Id` recibe el mismo valor de vuelta.
- **Alcanzable:** 3 h, una persona de P1.
- **Relevante:** trazabilidad exigida por la DoD.
- **Acotada:** 3 h · paso *observabilidad*. **Depende de:** T2 · **Traza:** CA2 de H03.
- **Estado (2026-09-16):** cerrada. Se implementó `RequestCorrelationFilter` (`@Component`,
  `OncePerRequestFilter`, precedencia máxima): preserva `X-Request-Id` o genera un UUID si viene
  ausente, seteándolo en la cabecera HTTP de respuesta antes de invocar la cadena (sobrevive a fallos
  y excepciones). Extrae `traceId` a `MDC` si viene `traceparent` y lo limpia en `finally`. Verificado
  con `RequestCorrelationFilterTest`.

### T8 — Pruebas de contrato del Gateway y CI en verde

- **Específica:** escribir las pruebas de contrato del Gateway con WireMock y dejar el
  pipeline de CI del servicio en verde (build, pruebas, cobertura).
- **Medible:** las pruebas de contrato pasan y el pipeline está en verde.
- **Alcanzable:** 4 h, una persona de P1.
- **Relevante:** evidencia de que el borde funciona como se acordó.
- **Acotada:** 4 h · paso *prueba E2E*. **Depende de:** T3–T7 · **Traza:** CA6 de H03.
- **Estado (2026-09-16):** diferida. Por convención del repositorio, el pipeline de CI vive
  únicamente en la rama `feat/qa-gate` (no se crean workflows en `lara`). `mvn test` pasa 100% limpio
  en local (213 tests verdes), asegurando que el build y los tests pasarán inmediatamente al
  conectarse el CI.

> **Total H03:** 34 h (+ 3 h de T9).

> **Revisión 2026-09-21 (tras la integración `main` → `dev`):**
> - **T2:** el ADR vigente es [ADR-003](../../../../00-gobierno-y-evolucion/adr/ADR-003-estructura-multimodulo-y-variables-de-entorno.md); `ArchitectureTest` corre en `app/src/test`.
> - **T3:** el «verificado en los 13 controladores» quedó viejo: hay **30** `@RestController` en `app`. Todos los de `/api/llm/**`
>   usan `${app.api.private-path}`; moderación además publica `/moderation/v1/**` sin prefijo (ruta doble, por contrato de EP-08).
>   El registro en Eureka y el ruteo por el Gateway siguen sin verificarse de punta a punta.
> - **T4:** el servicio **no valida `aud=llm-service`**: confía en los headers `X-*` que arma el Gateway (ADR-001 §3);
>   el JWT y su audiencia los valida el Gateway. El `401` por scope faltante sí está implementado. El scope del tutor es `llm.tutor.interact`.
> - **T8:** sigue diferida; el workflow de CI se retiró (`28a8b5c1`), así que CA6 solo se cumple corriendo `mvn verify` localmente.

---

## Deuda post-auditoría (fuera de las 34 h originales)

> Agregada el 2026-09-12 contra el [checklist de cierre de S1](../../../../01-vision-alcance-y-entrega/03-entregas/checklist-cierre-s1.md)
> §1.4 y §2.2 — no estaba prevista en la planificación original de H03 porque el hueco se detectó
> auditando el código contra la ficha, no al planificarla.

### T9 — 🔴 Mapear `ResourceNotFoundException` a `404` real

- **Específica:** agregar a `ApiExceptionHandler` un mapeo de `ResourceNotFoundException` (nueva)
  a `404 Not Found`, y reemplazar los `IllegalStateException` que hoy señalan "no existe" por esa
  excepción — dejando `409` únicamente para conflictos de estado reales (versión ya publicada,
  calibración activa duplicada).
- **Medible:** pedir un recurso inexistente (golden set, rúbrica, calibración) por un id bien
  formado pero que no existe responde `404` *Problem Details*, nunca `409` ni `500`.
- **Alcanzable:** 3 h, una persona de P1.
- **Relevante:** cierra el hallazgo `CORRECCIONES-SUGERIDAS.md` ítem 4 y hace reales CA5/CA6 de
  H06 (histórica) y el criterio equivalente de `s02-h02` — hoy ningún endpoint real cumple esto.
- **Acotada:** 3 h · paso *observabilidad*. **Depende de:** T6 · **Traza:**
  [`checklist-cierre-s1.md` §1.4](../../../../01-vision-alcance-y-entrega/03-entregas/checklist-cierre-s1.md).
- **Estado (2026-09-21):** cerrada. Se creó `application/exception/ResourceNotFoundException`, mapeada en
  `ApiExceptionHandler` a `404` Problem Details (`error: not_found`, con `requestId`). Reemplaza al
  `IllegalStateException` en los 11 puntos donde el recurso simplemente no existe (golden set, rúbrica,
  plantilla, calibración, corrida de shadow, modelo evaluador). Los mensajes mixtos «no existe **o** ya no
  es un borrador / no está publicada» y los `update` condicionales por estado siguen en `409`. Cubierta por
  `ApiExceptionHandlerTest`, `ShadowRunServiceTest` y tres ITs (`GoldenSetFlowIT`, `CredentialFlowIT`,
  `CalibrationFlowIT`) que antes esperaban `409`. El OpenAPI ya declaraba `404` en esos endpoints: el código
  ahora lo cumple. Cierra el ítem 4 de `CORRECCIONES-SUGERIDAS.md` y H06·CA5.
