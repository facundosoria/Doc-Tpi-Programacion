# LLM-S01-H01 — ADR de arquitectura y convenciones técnicas

- **Estado:** 🟢 Cerrada el 2026-09-19 y **re-verificada el 2026-09-21** tras la integración `main` → `dev`. ADR-001 y ADR-002 aceptados por aprobación directa de Facundo Soria (sin PR formal); la integración dejó ADR-001 parcialmente desactualizado y se rehízo su contenido en **ADR-003**.
- **Ficha:** [`historias/ep-01/h01.md`](../../../07-planificacion-y-trabajo-equipo/09-epicas-historias-tareas-sprints/historias/ep-01/h01.md) · **Tareas:** [`tareas/ep-01/h01.md`](../../../07-planificacion-y-trabajo-equipo/09-epicas-historias-tareas-sprints/tareas/ep-01/h01.md)
- **ADR vigentes:** [ADR-001](../../../00-gobierno-y-evolucion/adr/ADR-001-arquitectura-y-convenciones-llm-service.md) (§3 identidad),
  [ADR-002](../../../00-gobierno-y-evolucion/adr/ADR-002-mensajeria-kafka-outbox-y-dedup.md),
  [ADR-003](../../../00-gobierno-y-evolucion/adr/ADR-003-estructura-multimodulo-y-variables-de-entorno.md) (paquetes, dependencias y variables). El «ADR-019» del registro consolidado quedó histórico.
- **Prueba de arquitectura:** `app/src/test/java/ar/edu/utn/frc/tup/piv/llm/architecture/ArchitectureTest.java`
  (`archunit-junit5`, módulo `app`).

## Veredicto por tarea (revisión 2026-09-21)

| Tarea | Estado | Evidencia / hallazgo |
|---|---|---|
| T1 Relevar convenciones | 🟡 | El ADR-001 no lista convenciones candidatas con su antecedente; el entregable es débil, no roto por el merge |
| T2 Árbol de módulos y regla | 🟢 (rehecha) | ADR-001 §1–2 describía `api/`, `security/` e `infrastructure/` que ya no existen; ADR-003 §1–3 documenta el reactor y `adapter/in|out` |
| T3 Variables de entorno | 🟢 (rehecha) | Faltaban 12 variables (`LLM_COURSES_*`, `LLM_GATEWAY_*`, `LLM_EVALUATION_*`, `NOTIFICATIONS_*`) y `KAFKA_BOOTSTRAP` figuraba como `KAFKA_BOOTSTRAP_SERVERS`; ADR-003 §4 y `.env.example` sincronizados |
| T4 ArchUnit | 🟢 con deuda | 11 reglas en verde (`mvn -o test -pl app -am -Dtest=ArchitectureTest`, 2026-09-21); 0 imports prohibidos en `domain`; se sumó `domainDoesNotDependOnAdapters`. Deuda: 10 controllers exentos de `webAdapterDoesNotDependOnPersistenceAdapter` |
| T5 Revisión y publicación | 🟡 | ADR publicados y aceptados; el «enlace desde el CI» no se puede cumplir: `llm-service-ci.yml` se retiró el 2026-09-19 (`28a8b5c1`) |

## Veredicto por CA

| CA | Estado |
|---|---|
| CA1 — ADR publicado con fecha y autores | ✅ ADR-001/002/003 (aprobación directa, sin PR formal) |
| CA2 — enumera los paquetes y la regla de dependencias | ✅ ADR-003 §2–3 (el ADR-001 §1–2 quedó superado) |
| CA3 — tabla de variables de entorno | ✅ ADR-003 §4, contrastada con `application*.yml` |
| CA4 (neg.) — import de Spring en `domain` rompe la verificación | ✅ `ArchitectureTest` |
| CA5 (neg.) — variable no listada bloquea el PR | 🟡 política de revisión, sin chequeo automático. Se incumplió durante la integración (las variables de courses entraron sin ADR); un test que compare `application*.yml` con el ADR lo cerraría |

## Pendiente

- **Referente de producto** de la ficha sin nombrar (dato que no está en el repo).
- Sin CI: CA4 solo se garantiza corriendo `mvn test` localmente.
- `LLM_COURSES_BASE_URL` y `LLM_COURSES_SERVICE_TOKEN` no tienen default fuera del perfil `workbench`: el servicio no arranca sin ellas (relevante para H02).

## Re-verificación 2026-09-23

`mvn verify` (BUILD SUCCESS, 106 IT, 0 fallos): `ArchitectureTest` 11/11 reglas en verde. JaCoCo: `All coverage checks have been met`. Sin cambios en el resultado respecto a la verificación del 2026-09-21; la reserva de CA1 (aprobación sin PR formal) y CA5 (política no automatizada) persisten.

