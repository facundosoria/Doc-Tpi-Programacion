# Revisión de EP-01 · H07 tras la integración `main` → `dev` — 2026-09-21

## Decisión anterior

H07 figuraba 🟢 con `EventOutboxKafkaFlowIT` y la prueba de reinicio como evidencia.

## Motivo

La integración descartó el Kafka de `main` (`V24`/`V25`, `intento_cerrado.v1`) y conservó el de `dev`.
Se re-ejecutaron los tests de Kafka y la prueba de reinicio, y se contrastó el AsyncAPI con el código.

## Regla vigente

- El consumidor de `practice-events` solo evalúa; bloquear la asignación de calibración y encolar
  la evaluación pendiente quedan sin disparador hasta que Tema 05 incluya `challengeId` en `ATTEMPT_CLOSED`.
- Kafka no se habilita en la plataforma (`llm.kafka.enabled`) antes de cerrar ese punto.
- El AsyncAPI es válido pero declara operaciones no construidas (`COURSE_ARCHIVED`, `CALIBRATION_*`, `JAILBREAK_INCIDENT`).

## Fuentes

`mvn -o verify -Dintegration=true` (688 + 99), `test-compose-restart.sh`, `npx @asyncapi/cli validate`,
`messaging/kafka/**`, Javadoc de `PracticeAttemptClosedListener`.

## Documentos V2 corregidos

`historias/ep-01/h07.md` (links), `04-estado-de-implementacion/ep-01/h07.md` (nuevo), `ep-01/README.md`.
