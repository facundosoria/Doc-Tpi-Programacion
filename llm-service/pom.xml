<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.5.0</version>
    <relativePath/>
  </parent>
  <groupId>ar.edu.utn.frc.tup.piv</groupId>
  <artifactId>llm-service-parent</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <name>llm-service parent</name>
  <packaging>pom</packaging>
  <properties>
    <java.version>21</java.version>
    <spring-cloud.version>2025.0.0</spring-cloud.version>
    <langchain4j.version>1.20.0</langchain4j.version>
    <!-- 1.21.4+ habla con Docker Engine 29 (API mínima 1.44); 1.21.0 recibe 400 y no arranca. -->
    <testcontainers.version>1.21.4</testcontainers.version>
    <argLine></argLine>
    <failsafeArgLine></failsafeArgLine>
  </properties>
  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.cloud</groupId>
        <artifactId>spring-cloud-dependencies</artifactId>
        <version>${spring-cloud.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>dev.langchain4j</groupId>
        <artifactId>langchain4j-bom</artifactId>
        <version>${langchain4j.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>
  <modules>
    <module>provider-spi</module>
    <module>provider-openai-compatible</module>
    <module>provider-anthropic</module>
    <module>provider-gemini</module>
    <module>app</module>
  </modules>
</project>
