# Historias de usuario — Sprint 1 (fichas largas)

> **Qué es este documento.** Las historias de S1 en el [formato del template oficial de
> Historia de Usuario de la Wiki de Taiga](../plantillas/historia-de-usuario-taiga.md):
> Como / Quiero / Para, Notas, Criterios de Aceptación con negativos, BDD (≥ 3 escenarios),
> Prototipo, Estimación y Dependencias. Es la versión detallada de la tabla compacta de
> [`Plan de ejecucion/07` · sección «S1»](<../../Plan de ejecucion/07-backlog-ejecutable-sprints.md>).
>
> **Qué NO es.** No es fuente de verdad de planificación. Si un dato de acá no coincide:
>
> | Dato | Fuente única |
> |---|---|
> | ID, épica, pareja, dependencias, horas | [`Plan de ejecucion/07`](<../../Plan de ejecucion/07-backlog-ejecutable-sprints.md>) · «S1» |
> | Tipo (HU de valor / habilitador) y demo | [30 · §5](../30-arranque-agil-y-sprint-0.md) |
> | DoR / DoD | [23 · §9.2](../23-plan-construccion-producto-llm.md) |
> | Método para redactar y estimar | [29 · Guía de cátedra: Historias de Usuario](../29-guia-catedra-historias-de-usuario.md) |
> | Contrato HTTP y adenda S1 | [`contracts/`](../contracts/) · [adenda golden set](../contracts/llm-service-v1-s1-golden-set-adenda.md) |
> | Secuencia técnica de cada tarea | [playbook de construcción · §4](<../../Plan de ejecucion/06-playbook-de-construccion.md>) |
>
> **Título en Taiga.** Cada ficha se carga con el título `GXX — TÍTULO` (`GXX` = número de
> grupo, aún sin asignar). El ID interno `LLM-S01-Hyy` es el del equipo ([23 · §9.2](../23-plan-construccion-producto-llm.md)).
>
> **Tipo.** A pedido del equipo todas las historias de S1 están acá en formato largo. En el
> backlog de Taiga, **H05–H07** se cargan como **HU** (rol real, puntos de valor) y **H01–H04,
> H08, H09** como **tareas** bajo EP-01: fallan la **V** de INVEST (el `COMO` es «el equipo» o
> «la plataforma», no un rol que percibe el resultado). El formato largo acá es para trazar los
> escenarios de aceptación, no para forzar su carga como HU.
>
> **Estimación en puntos.** Ninguna ficha trae puntos Fibonacci: se asignan en el **Sprint 0**
> con Planning Poker contra la historia canónica (candidata: **LLM-S01-H06**). La columna *h* es
> la referencia de planificación del plan y **no se convierte** a puntos ([29 · §5–6](../29-guia-catedra-historias-de-usuario.md)).
>
> **Tareas.** Cada ficha cierra con un desglose en **tareas**: pasos técnicos internos que define
> el equipo de desarrollo, **sin** formato Como/Quiero/Para ([29 · §4](../29-guia-catedra-historias-de-usuario.md)).
> Ordenan la construcción según la secuencia del [playbook · §4](<../../Plan de ejecucion/06-playbook-de-construccion.md>)
> (contrato y amenaza → dominio y migración → caso de uso → adaptadores → seguridad y resiliencia →
> observabilidad → prueba E2E → demo y evidencia) y se parten en **una jornada efectiva o menos**
> ([23 · §9.2](../23-plan-construccion-producto-llm.md)). Las horas por tarea son **orientativas**:
> suman la columna *h* del plan, que sigue siendo la fuente. En Taiga se cargan como tareas hijas
> de la HU (H05–H07) o de la tarea de sprint (H01–H04, H08, H09).

---

## Índice

| ID                                                                             | Título                                            | Tipo                | Épica | Pareja | Dep.      |       h |
| ------------------------------------------------------------------------------ | ------------------------------------------------- | ------------------- | ----- | ------ | --------- | ------: |
| [LLM-S01-H01](#llm-s01-h01--adr-de-arquitectura-y-convenciones-tecnicas)       | ADR de arquitectura y convenciones técnicas       | Tarea (habilitador) | EP-01 | P1     | —         |      16 |
| [LLM-S01-H02](#llm-s01-h02--entorno-reproducible-con-un-comando)               | Entorno reproducible con un comando               | Tarea (habilitador) | EP-01 | P1     | H01       |      30 |
| [LLM-S01-H03](#llm-s01-h03--esqueleto-transversal-del-servicio)                | Esqueleto transversal del servicio                | Tarea (habilitador) | EP-01 | P1     | H02       |      34 |
| [LLM-S01-H04](#llm-s01-h04--esquema-inicial-versionado-con-auditoría)          | Esquema inicial versionado con auditoría          | Tarea (habilitador) | EP-01 | P1     | H03       |      38 |
| [LLM-S01-H05](#llm-s01-h05--alta-de-golden-set-y-carga-de-entradas)            | Alta de golden set y carga de entradas            | **HU**              | EP-03 | P5     | H04       |      24 |
| [LLM-S01-H06](#llm-s01-h06--consulta-del-golden-set-que-sobrevive-al-reinicio) | Consulta del golden set que sobrevive al reinicio | **HU** *(canónica)* | EP-03 | P5     | H04       |      14 |
| [LLM-S01-H07](#llm-s01-h07--pantalla-docente-mínima-del-golden-set)            | Pantalla docente mínima del golden set            | **HU**              | EP-03 | P5     | H05, H06  |      24 |
| [LLM-S01-H08](#llm-s01-h08--contrato-openapi-y-mock-del-golden-set-publicados) | Contrato OpenAPI y mock del golden set publicados | Tarea (habilitador) | EP-01 | P1     | H03       |      10 |
| [LLM-S01-H09](#llm-s01-h09--suite-de-pruebas-y-guía-de-demo-de-s1)             | Suite de pruebas y guía de demo de S1             | Tarea (habilitador) | EP-01 | todos  | H04–H07   |      18 |
|                                                                                |                                                   |                     |       |        | **Total** | **208** |

**Demo de S1:** un docente autorizado crea un golden set, carga una entrada y la consulta
después de reiniciar el servicio.

---

# LLM-S01-H01 — ADR de arquitectura y convenciones técnicas

| | |
|---|---|
| **Épica** | EP-01 · Plataforma, contratos e integración |
| **Pareja** | P1 |
| **Dependencias** | — |
| **Estimación (plan)** | 16 h |
| **Tipo** | Tarea / habilitador técnico (falla la **V** de INVEST) |
| **Requisito** | RF-NFR-01/09/10 (orientativo; traza fina en [21](../21-matriz-trazabilidad-llm.md)) |
| **Referente de producto** | *(a nombrar en Sprint 0)* |

## Descripción (Como / Quiero / Para)

- **Como:** equipo de desarrollo del `llm-service`
- **Quiero:** un ADR con el árbol de módulos y las convenciones técnicas del servicio
- **Para:** que las cinco parejas construyan sobre las mismas fronteras y decisiones

## Notas / Observaciones

- **Reglas de negocio:** el ADR fija Java 21, Spring Boot 3 / Maven y los límites entre paquetes
  `api` · `application` · `domain` · `infrastructure` · `security` · `configuration`. El paquete
  `domain` **no** depende de Spring, Kafka ni de SDKs de proveedores de modelos.
- **Validaciones:** las fronteras de paquete se pueden verificar de forma automática (ArchUnit) o
  en revisión de PR.
- **Datos obligatorios:** lista de variables de entorno con nombre, propósito y valor por defecto;
  decisión registrada con fecha, alternativas descartadas y responsables.
- **Performance:** no aplica.
- **Seguridad:** el ADR nombra dónde viven los secretos (fuera del repo) y cómo se inyectan.
- **Accesibilidad:** no aplica (documento interno).
- **Otros:** el ADR queda versionado en el repositorio; cambiarlo exige un nuevo ADR que supersede
  al anterior, no editar el texto.

## Criterios de Aceptación (CA)

- **CA1:** existe un ADR publicado y aprobado por el equipo en el repositorio, con fecha y autores.
- **CA2:** el ADR enumera los seis paquetes y la regla de dependencias entre ellos.
- **CA3:** el ADR incluye la tabla de variables de entorno del servicio.
- **CA4 (negativo):** un PR que importe Spring, Kafka o un SDK de proveedor dentro de `domain` es
  rechazado por CI o por revisión, citando el ADR.
- **CA5 (negativo):** usar una variable de entorno no listada en el ADR se marca en revisión.

## BDD (mínimo 3 escenarios)

**Característica:** convenciones de arquitectura compartidas por todas las parejas.

### Escenario 1 — El ADR habilita el arranque parejo

- **Dado:** el ADR está aprobado y publicado en el repositorio
- **Cuando:** una pareja crea el módulo que le toca
- **Entonces:** su estructura de paquetes coincide con la del ADR y la revisión no observa desvíos

### Escenario 2 — Se protege la independencia del dominio

- **Dado:** el ADR prohíbe dependencias de framework en `domain`
- **Cuando:** un PR agrega `import org.springframework...` en una clase de `domain`
- **Entonces:** la verificación de arquitectura falla y el PR no se puede mergear

### Escenario 3 — Configuración fuera del ADR

- **Dado:** el ADR lista las variables de entorno admitidas
- **Cuando:** un cambio lee una variable de entorno que el ADR no documenta
- **Entonces:** la revisión marca la observación y pide actualizar el ADR antes de aprobar

## Prototipo

- **Capturas:** no aplica (documento).
- **URL Figma / Storybook / Mock API:** no aplica.

## Estimación / Prioridad

| Puntos (Fibonacci) | Prioridad (MoSCoW) |
|---|---|
| *(a asignar en Sprint 0)* | Must |

## Dependencias / Impactos

- **Servicios involucrados:** `llm-service` (todos sus módulos).
- **Módulos afectados:** todos.
- **Otros equipos / aprobaciones:** ninguna externa.
- **Impacto en datos / migraciones:** ninguno.
- **Riesgos y mitigación:** un ADR tardío obliga a refactor; se resuelve en el primer día de S1 y
  bloquea el resto de las historias hasta estar aprobado.

## Tareas

> Pasos técnicos del equipo (P1). Se cargan en Taiga como tareas de la tarea de sprint. Horas orientativas.

| #   | Tarea                                                                                                                              | Foco              |      h |
| --- | ---------------------------------------------------------------------------------------------------------------------------------- | ----------------- | -----: |
| T1  | Relevar convenciones candidatas (Java 21, Boot 3/Maven, ArchUnit) y antecedentes de `docs/importado/`                              | Descubrimiento    |      3 |
| T2  | Redactar el árbol de módulos (`api`/`application`/`domain`/`infrastructure`/`security`/`configuration`) y la regla de dependencias | Redacción del ADR |      4 |
| T3  | Definir la tabla de variables de entorno (nombre, propósito, valor por defecto) y dónde viven los secretos                         | Redacción del ADR |      3 |
| T4  | Prueba de arquitectura ArchUnit que falla si `domain` importa Spring/Kafka/SDK (o checklist de PR si se difiere)                   | Verificación      |      3 |
| T5  | Revisión y aprobación del equipo; publicar el ADR versionado y enlazarlo desde el CI                                               | Cierre            |      3 |
|     | **Total**                                                                                                                          |                   | **16** |

---

# LLM-S01-H02 — Entorno reproducible con un comando

| | |
|---|---|
| **Épica** | EP-01 · Plataforma, contratos e integración |
| **Pareja** | P1 |
| **Dependencias** | H01 |
| **Estimación (plan)** | 30 h |
| **Tipo** | Tarea / habilitador técnico |
| **Requisito** | RF-NFR-03/04 (orientativo) |
| **Referente de producto** | *(a nombrar en Sprint 0)* |

## Descripción (Como / Quiero / Para)

- **Como:** desarrollador de cualquier pareja
- **Quiero:** levantar todo el entorno local con un solo comando
- **Para:** trabajar sin instalar ni configurar dependencias a mano

## Notas / Observaciones

- **Reglas de negocio:** `docker compose up` deja *healthy* PostgreSQL y —según lo que pida la
  plataforma— Eureka/Gateway y Kafka. `up`, verificación de *health* y `down` están documentados.
- **Validaciones:** cada servicio del Compose define su `healthcheck`; el comando no se considera
  exitoso hasta que todos reportan *healthy*.
- **Datos obligatorios:** archivo `compose.yaml`, `.env.example` con las variables del ADR (H01),
  sección del README con los tres comandos.
- **Performance:** el entorno arranca en frío en un tiempo razonable en una notebook del equipo.
- **Seguridad:** el Compose usa credenciales de desarrollo, nunca secretos reales; el `.env` real
  está en `.gitignore`.
- **Accesibilidad:** no aplica.
- **Otros:** si falta Docker o `docker compose`, el arranque falla con un mensaje que nombra el
  requisito; no deja el entorno a medias.

## Criterios de Aceptación (CA)

- **CA1:** en una máquina limpia con Docker, `docker compose up` deja todos los servicios
  *healthy*.
- **CA2:** el README documenta `up`, comprobación de *health* y `down`, y funcionan.
- **CA3 (negativo):** sin Docker instalado, el comando falla con un mensaje claro que indica qué
  falta, y no arranca servicios parciales.
- **CA4 (negativo):** con un puerto requerido ya ocupado, el error identifica el puerto en
  conflicto.

## BDD (mínimo 3 escenarios)

**Característica:** entorno local reproducible.

### Escenario 1 — Arranque en máquina limpia

- **Dado:** una máquina con Docker y el repositorio clonado, sin otras dependencias instaladas
- **Cuando:** el desarrollador ejecuta `docker compose up`
- **Entonces:** PostgreSQL (y Eureka/Gateway/Kafka si la plataforma los pide) quedan *healthy*
- **Y:** el desarrollador puede llamar al servicio sin instalar nada más

### Escenario 2 — Falta Docker

- **Dado:** una máquina sin Docker
- **Cuando:** el desarrollador ejecuta el comando de arranque
- **Entonces:** el comando falla e indica que Docker es un requisito, sin dejar contenedores a medias

### Escenario 3 — Puerto ocupado

- **Dado:** el puerto de PostgreSQL ya está en uso por otro proceso
- **Cuando:** el desarrollador ejecuta `docker compose up`
- **Entonces:** el arranque falla con un mensaje que nombra el puerto en conflicto

## Prototipo

- **Capturas:** salida de consola esperada de `up` / `down` (adjuntar en la ficha de Taiga).
- **URL Figma / Storybook / Mock API:** no aplica.

## Estimación / Prioridad

| Puntos (Fibonacci) | Prioridad (MoSCoW) |
|---|---|
| *(a asignar en Sprint 0)* | Must |

## Dependencias / Impactos

- **Servicios involucrados:** PostgreSQL, Eureka/Gateway y Kafka (imágenes de la plataforma).
- **Módulos afectados:** raíz del repositorio (`compose.yaml`, README).
- **Otros equipos / aprobaciones:** responsables de Gateway/Eureka y de PostgreSQL local (D01).
- **Impacto en datos / migraciones:** el volumen de PostgreSQL debe persistir entre reinicios
  (necesario para H06).
- **Riesgos y mitigación:** divergencia con la plataforma real; se acota levantando solo lo que la
  plataforma expone y documentando cada servicio en [26](../26-herramientas-y-librerias.md).

## Tareas

> Pasos técnicos del equipo (P1). Se cargan en Taiga como tareas de la tarea de sprint. Horas orientativas.

| #  | Tarea                                                                                                      | Foco                    |   h |
| -- | -------------------------------------------------------------------------------------------------------- | ----------------------- | --: |
| T1 | `compose.yaml` con PostgreSQL, volumen persistente (necesario para H06) y `healthcheck`                   | Persistencia            |   5 |
| T2 | Agregar Eureka/Gateway y Kafka según lo que exponga la plataforma, cada uno con su `healthcheck`          | Plataforma              |   6 |
| T3 | `.env.example` con las variables del ADR (H01); `.env` real en `.gitignore`, credenciales solo de dev     | Configuración           |   3 |
| T4 | Arranque que falla con mensaje claro si falta Docker / `docker compose` y no deja servicios parciales      | Robustez                |   4 |
| T5 | Detección de puerto ocupado con un error que nombra el puerto en conflicto                                 | Robustez                |   3 |
| T6 | Sección del README con `up`, verificación de *health* y `down`, verificados en una máquina limpia          | Documentación           |   4 |
| T7 | Prueba de humo del arranque en frío y medición del tiempo en una notebook del equipo                       | Verificación            |   5 |
|    | **Total**                                                                                                 |                         | **30** |

---

# LLM-S01-H03 — Esqueleto transversal del servicio

| | |
|---|---|
| **Épica** | EP-01 · Plataforma, contratos e integración |
| **Pareja** | P1 |
| **Dependencias** | H02 |
| **Estimación (plan)** | 34 h |
| **Tipo** | Tarea / habilitador técnico |
| **Requisito** | RF-NFR-01/03/04; contratos v1 (orientativo) |
| **Referente de producto** | *(a nombrar en Sprint 0)* |

## Descripción (Como / Quiero / Para)

- **Como:** plataforma «Aprendizaje Gamificado de Programación» (equipos consumidores)
- **Quiero:** que el `llm-service` exponga su esqueleto transversal por el Gateway
- **Para:** integrar contra un borde estable antes de que existan las funciones de IA

## Notas / Observaciones

- **Reglas de negocio:** el servicio se registra en Eureka como `llm-service`; el Gateway rutea
  `/api/llm/**` sin reescritura de path; la seguridad M2M valida `aud=llm-service` y el scope
  requerido; los errores se devuelven como *Problem Details* (RFC 7807).
- **Validaciones:** se propaga la correlación `traceparent` y `X-Request-Id` en cada respuesta; un
  token sin el scope necesario se rechaza con `401`; una identidad delegada falsificada o mal
  formada se rechaza con `403`.
- **Datos obligatorios (headers de borde):** `X-Service-Id`, `X-Service-Scopes`,
  `X-Delegated-User`, `X-Request-Id`, `traceparent`.
- **Performance:** el *health* de Actuator responde sin llamar a proveedores externos.
- **Seguridad:** ningún endpoint funcional queda accesible sin pasar por el Gateway; los logs no
  registran tokens.
- **Accesibilidad:** no aplica.
- **Otros:** el pipeline de CI del servicio queda en verde (build, pruebas, cobertura).
- **Endpoints expuestos en esta historia:** `GET /actuator/health` (liveness/readiness) y la ruta
  base `/api/llm/**` protegida; las operaciones de golden set llegan en H05/H06.

## Criterios de Aceptación (CA)

- **CA1:** `llm-service` aparece registrado en Eureka y responde a través del Gateway en
  `/api/llm/**`.
- **CA2:** una request con token M2M válido (`aud=llm-service`) y scope correcto obtiene respuesta
  y recibe de vuelta su `X-Request-Id`.
- **CA3:** los errores se serializan como *Problem Details* con `type`, `title`, `status` y
  `detail`.
- **CA4 (negativo):** token sin el scope requerido → `401` *Problem Details*.
- **CA5 (negativo):** header de identidad delegada ausente o falsificado → `403`.
- **CA6:** el pipeline de CI del servicio está en verde.

## BDD (mínimo 3 escenarios)

**Característica:** borde transversal del `llm-service`.

### Escenario 1 — Integración contra un borde estable

- **Dado:** `llm-service` registrado en Eureka y el Gateway ruteando `/api/llm/**`
- **Cuando:** un servicio consumidor llama con token M2M válido y scope correcto
- **Entonces:** recibe una respuesta del servicio
- **Y:** la respuesta incluye el mismo `X-Request-Id` que envió

### Escenario 2 — Token sin scope

- **Dado:** un servicio consumidor con un token M2M válido pero sin el scope requerido
- **Cuando:** llama a una ruta `/api/llm/**`
- **Entonces:** recibe `401` con cuerpo *Problem Details*

### Escenario 3 — Identidad delegada falsificada

- **Dado:** una request que trae un `X-Delegated-User` que no vino del Gateway de confianza
- **Cuando:** llega al servicio
- **Entonces:** se rechaza con `403` y no se ejecuta ninguna acción

## Prototipo

- **Capturas:** no aplica (borde técnico).
- **Mock API / Swagger:** `GET /actuator/health`; contrato base en [`contracts/llm-service-v1.openapi.yaml`](../contracts/llm-service-v1.openapi.yaml).

## Estimación / Prioridad

| Puntos (Fibonacci) | Prioridad (MoSCoW) |
|---|---|
| *(a asignar en Sprint 0)* | Must |

## Dependencias / Impactos

- **Servicios involucrados:** Gateway, Eureka, emisor de tokens M2M.
- **Módulos afectados:** `api`, `security`, `configuration`.
- **Otros equipos / aprobaciones:** equipo de Gateway/Discovery (D01): ruta, `aud` y scopes.
- **Impacto en datos / migraciones:** ninguno.
- **Riesgos y mitigación:** el contrato de headers del Gateway puede cambiar; se aísla en el
  módulo `security` y se cubre con pruebas de contrato (WireMock).

## Tareas

> Pasos técnicos del equipo (P1), en el orden del [playbook · §4](<../../Plan de ejecucion/06-playbook-de-construccion.md>). Horas orientativas.

| #  | Tarea                                                                                                          | Paso (playbook §4)       |   h |
| -- | ---------------------------------------------------------------------------------------------------------- | ----------------------- | --: |
| T1 | Contrato y amenaza: headers de borde (`X-Service-Id`, `X-Service-Scopes`, `X-Delegated-User`, `X-Request-Id`, `traceparent`), actor M2M y fallas | Contrato y amenaza |   4 |
| T2 | Estructura de paquetes del ADR + configuración base Spring Boot 3 y Actuator (`health` sin llamar a externos)  | Adaptadores             |   4 |
| T3 | Registro en Eureka como `llm-service` y ruta `/api/llm/**` por el Gateway sin reescritura de path              | Adaptadores             |   5 |
| T4 | Seguridad M2M: validar `aud=llm-service` y el scope requerido; token sin scope → `401` *Problem Details*        | Seguridad y resiliencia |   6 |
| T5 | Identidad delegada: rechazar `X-Delegated-User` ausente o falsificado con `403`, sin ejecutar la acción         | Seguridad y resiliencia |   4 |
| T6 | Manejador global *Problem Details* (RFC 7807) con `type`/`title`/`status`/`detail`; logs sin tokens             | Observabilidad          |   4 |
| T7 | Correlación: propagar `traceparent` y devolver el mismo `X-Request-Id` en cada respuesta                        | Observabilidad          |   3 |
| T8 | Pruebas de contrato del Gateway (WireMock) y pipeline CI del servicio en verde (build, pruebas, cobertura)      | Prueba E2E              |   4 |
|    | **Total**                                                                                                     |                         | **34** |

---

# LLM-S01-H04 — Esquema inicial versionado con auditoría

| | |
|---|---|
| **Épica** | EP-01 · Plataforma, contratos e integración |
| **Pareja** | P1 |
| **Dependencias** | H03 |
| **Estimación (plan)** | 38 h |
| **Tipo** | Tarea / habilitador técnico |
| **Requisito** | RF-IA-30 a 36 (golden set y calibración); RF-NFR-01 |
| **Referente de producto** | *(a nombrar en Sprint 0)* |

## Descripción (Como / Quiero / Para)

- **Como:** plataforma (dueña del dato académico)
- **Quiero:** el esquema inicial del `llm-service` versionado con migraciones
- **Para:** que el dato de referencia nazca con auditoría y sin edición destructiva

## Notas / Observaciones

- **Reglas de negocio:** una migración Flyway (`V1`) desde base vacía crea la rúbrica de
  plataforma **1.0** con sus **cinco dimensiones** y pesos (autonomía 30, claridad 25, progresión
  20, cumplimiento 15, eficiencia 10), y las tablas de golden set, entrada, idempotencia y
  auditoría.
- **Validaciones:** las tablas de dato académico son *append-only* — un `UPDATE` o `DELETE`
  destructivo se rechaza a nivel base. Una puntuación de referencia incompleta (menos de las cinco
  dimensiones) o fuera de rango (0–100) se rechaza. El índice de idempotencia es único por
  `(operación, servicio, usuario delegado, Idempotency-Key)`.
- **Datos obligatorios:** versión de rúbrica, idioma (`es`), definición de dimensiones y pesos.
- **Performance:** la migración corre en segundos sobre base vacía y es reproducible.
- **Seguridad:** la auditoría guarda actor (servicio + usuario), recurso, `X-Request-Id` y
  `traceparent`; no guarda transcripciones como texto libre interpretable.
- **Accesibilidad:** no aplica.
- **Otros:** cambiar la rúbrica exige una versión nueva, no editar la 1.0.

## Criterios de Aceptación (CA)

- **CA1:** Flyway sobre una base vacía deja creadas las tablas de rúbrica, dimensiones, golden
  set, entrada, idempotencia y auditoría, y la rúbrica 1.0 con sus cinco dimensiones y pesos.
- **CA2:** la migración es reproducible: correrla de nuevo desde cero da el mismo resultado.
- **CA3 (negativo):** un `UPDATE` o `DELETE` sobre una tabla *append-only* falla con un error que
  dice que la tabla no admite cambios.
- **CA4 (negativo):** insertar una entrada con cuatro dimensiones, o con un score de 120, se
  rechaza por restricción de la base.
- **CA5 (negativo):** dos registros de idempotencia con la misma clave para la misma operación,
  servicio y usuario no pueden coexistir.

## BDD (mínimo 3 escenarios)

**Característica:** esquema académico versionado e inmutable.

### Escenario 1 — Migración desde base vacía

- **Dado:** una base de datos PostgreSQL vacía
- **Cuando:** el servicio arranca y Flyway aplica la migración `V1`
- **Entonces:** existen las tablas del golden set y la rúbrica 1.0 con sus cinco dimensiones y pesos

### Escenario 2 — La tabla no admite edición destructiva

- **Dado:** un golden set ya persistido
- **Cuando:** alguien ejecuta `UPDATE golden_sets ...` o `DELETE FROM golden_sets ...`
- **Entonces:** la base rechaza la operación indicando que la tabla es *append-only*

### Escenario 3 — Puntuación de referencia inválida

- **Dado:** la restricción de que una entrada tiene las cinco dimensiones con valores 0–100
- **Cuando:** se intenta insertar una entrada con solo cuatro dimensiones (o un valor 120)
- **Entonces:** la inserción se rechaza por restricción y no queda registro

## Prototipo

- **Capturas:** diagrama entidad-relación de las tablas creadas (adjuntar en Taiga; ver
  [12 · Almacenamiento e ingesta](../12-almacenamiento-e-ingesta.md)).
- **Mock API / Swagger:** no aplica (capa de datos).

## Estimación / Prioridad

| Puntos (Fibonacci) | Prioridad (MoSCoW) |
|---|---|
| *(a asignar en Sprint 0)* | Must |

## Dependencias / Impactos

- **Servicios involucrados:** PostgreSQL.
- **Módulos afectados:** `infrastructure` (persistencia y migraciones), `domain` (invariantes).
- **Otros equipos / aprobaciones:** definición docente de las cinco dimensiones y pesos (D02).
- **Impacto en datos / migraciones:** crea el esquema base; todas las historias siguientes
  dependen de él.
- **Riesgos y mitigación:** si las dimensiones o pesos cambian tras S1, se emite una rúbrica
  versionada nueva; la 1.0 no se toca.

## Tareas

> Pasos técnicos del equipo (P1), en el orden del [playbook · §4](<../../Plan de ejecucion/06-playbook-de-construccion.md>). Horas orientativas.

| #  | Tarea                                                                                                          | Paso (playbook §4)       |   h |
| -- | ---------------------------------------------------------------------------------------------------------- | ----------------------- | --: |
| T1 | Modelar en `domain` las invariantes: rúbrica/dimensiones/pesos, golden set, entrada, idempotencia y auditoría | Dominio y migración     |   6 |
| T2 | Migración Flyway `V1` desde base vacía: tablas + rúbrica 1.0 (5 dimensiones, pesos 30/25/20/15/10, idioma `es`) | Dominio y migración   |   8 |
| T3 | Restricción *append-only* a nivel base en las tablas de dato académico (rechazo de `UPDATE`/`DELETE` destructivo) | Dominio y migración |   6 |
| T4 | CHECK de puntuación de referencia: exactamente las cinco dimensiones, valores enteros 0–100                     | Dominio y migración     |   4 |
| T5 | Índice único de idempotencia por `(operación, servicio, usuario delegado, Idempotency-Key)`                     | Dominio y migración     |   4 |
| T6 | Tabla de auditoría: actor (servicio + usuario), recurso, `X-Request-Id`, `traceparent`; sin transcripción como texto libre | Observabilidad |   4 |
| T7 | Prueba de integración Testcontainers: migración reproducible desde cero + negativos (4 dimensiones, score 120, clave duplicada) | Prueba E2E |   6 |
|    | **Total**                                                                                                     |                         | **38** |

---

# LLM-S01-H05 — Alta de golden set y carga de entradas

| | |
|---|---|
| **Épica** | EP-03 · Golden set y referencia humana |
| **Pareja** | P5 (con P4 en la definición de rúbrica) |
| **Dependencias** | H04 |
| **Estimación (plan)** | 24 h |
| **Tipo** | **Historia de usuario** (HU de valor) |
| **Requisito** | RF-IA-30 a 36 (golden set y calibración) |
| **Referente de producto** | *(a nombrar en Sprint 0)* |

## Descripción (Como / Quiero / Para)

- **Como:** docente autorizado de una cohorte
- **Quiero:** dar de alta un golden set y cargar entradas con su puntuación de referencia
- **Para:** armar la referencia humana que después calibra el evaluador de mi curso

## Notas / Observaciones

- **Reglas de negocio:** el alta y la carga se hacen por el Gateway (`/api/llm/**`). El servicio
  valida el **actor** (servicio de confianza + scope `llm.golden-set.manage` + usuario delegado),
  el **ownership de la cohorte** y la **idempotencia**. Cada operación de escritura exige el header
  `Idempotency-Key` (UUID) y queda registrada en auditoría.
- **Validaciones:** `transcript` es un arreglo JSON con al menos un mensaje y **no se interpreta
  como instrucciones**; `referenceScores` trae exactamente las cinco dimensiones con valores
  enteros 0–100; `language` es `es`; `rubricVersion` existe.
- **Datos obligatorios:** para el alta, `rubricVersion` y `language`; para la entrada,
  `transcript` y `referenceScores` (autonomía, claridad, progresión, cumplimiento, eficiencia).
- **Performance:** operaciones sincrónicas de baja latencia; sin llamadas a proveedores de modelo.
- **Seguridad:** un actor sin el scope requerido o una identidad no delegada por el Gateway →
  `403`; un docente que no es dueño de la cohorte → `403`; nunca se accede sin pasar por el
  Gateway.
- **Accesibilidad:** no aplica en esta historia (la pantalla es H07).
- **Otros:** el `id` del recurso creado se devuelve en el cuerpo y en el header `Location`.
- **Endpoints:**
  - `POST /api/llm/golden-sets` → `201` + `Location`; cuerpo `{ id, version, rubricVersion, language, createdAt }`.
  - `POST /api/llm/golden-sets/{goldenSetId}/entries` → `201` + `Location`; cuerpo `{ id, goldenSetId }`.

## Criterios de Aceptación (CA)

- **CA1:** un docente autorizado de su cohorte crea un golden set y le agrega una entrada válida;
  ambas respuestas son `201` con `Location`.
- **CA2:** repetir el mismo `POST` con la misma `Idempotency-Key` devuelve el mismo resultado sin
  crear un segundo recurso.
- **CA3 (negativo):** un docente que no es dueño de la cohorte recibe `403` y no se crea nada.
- **CA4 (negativo):** una entrada con `referenceScores` incompleto (cuatro dimensiones) o fuera de
  rango recibe `400` *Problem Details*.
- **CA5 (negativo):** una request que no pasa por el Gateway (sin headers de identidad delegada) →
  `403`.

## BDD (mínimo 3 escenarios)

**Característica:** construcción del golden set por el docente.

### Escenario 1 — Alta y carga por el docente dueño (camino feliz)

- **Dado:** un docente autorizado sobre su cohorte, autenticado por el Gateway
- **Cuando:** crea un golden set y luego carga una entrada con transcripción y las cinco
  puntuaciones de referencia
- **Entonces:** el sistema responde `201` en ambos casos y devuelve el `id` y el `Location` de
  cada recurso
- **Y:** el golden set queda visible al consultarlo (ver [H06](#llm-s01-h06--consulta-del-golden-set-que-sobrevive-al-reinicio))

### Escenario 2 — Segunda carga con la misma Idempotency-Key

- **Dado:** una entrada ya cargada con una `Idempotency-Key` determinada
- **Cuando:** el docente reenvía exactamente el mismo `POST` con la misma clave
- **Entonces:** el sistema devuelve la misma respuesta que la primera vez y no crea una segunda
  entrada

### Escenario 3 — Docente de otra cohorte

- **Dado:** un docente autenticado que no es dueño de la cohorte del golden set
- **Cuando:** intenta crear un golden set o cargar una entrada en esa cohorte
- **Entonces:** el sistema responde `403` y no persiste ningún dato

### Escenario 4 — Puntuación de referencia incompleta

- **Dado:** el formulario de carga de entrada
- **Cuando:** el docente envía `referenceScores` con solo cuatro dimensiones
- **Entonces:** el sistema responde `400` con *Problem Details* indicando qué falta, y no crea la
  entrada

## Prototipo

- **Capturas / Wireframes:** bocetos de baja fidelidad («Paso 1: crear golden set» y «Paso 2: cargar entrada») en [wireframes-h07-golden-set.md](../prototipos/wireframes-h07-golden-set.md); demo funcional en [`Demos/Golden Set`](../../Demos/Golden%20Set/README.md).
- **URL Figma:** *(pendiente)*
- **Storybook:** *(pendiente)*
- **Mock API / Swagger:** [adenda S1 del golden set](../contracts/llm-service-v1-s1-golden-set-adenda.md);
  `POST /api/llm/golden-sets`, `POST /api/llm/golden-sets/{goldenSetId}/entries`.

## Estimación / Prioridad

| Puntos (Fibonacci) | Prioridad (MoSCoW) |
|---|---|
| *(a asignar en Sprint 0, contra LLM-S01-H06)* | Must |

## Dependencias / Impactos

- **Servicios involucrados:** Gateway (identidad delegada), `admin-service` (contrato v1 + adenda),
  `courses-service` (definición de cohorte / ownership).
- **Módulos afectados:** `api`, `application`, `security`, `infrastructure`.
- **Otros equipos / aprobaciones:** `admin-service` debe congelar cualquier campo del contrato que
  hoy no exista antes de publicarlo; definición docente de las cinco dimensiones (D02).
- **Impacto en datos / migraciones:** usa el esquema de H04; escribe en `golden_sets`,
  `golden_set_entries`, `idempotency_requests` y `audit_events`.
- **Riesgos y mitigación:** el modelo de *ownership por cohorte* depende de `courses-service`; si no
  está listo, se acuerda un criterio provisional documentado y se congela con el contrato.

## Tareas

> Pasos técnicos del equipo (P5), en el orden del [playbook · §4](<../../Plan de ejecucion/06-playbook-de-construccion.md>). Se cargan en Taiga como tareas hijas de la HU. Horas orientativas.

| #  | Tarea                                                                                                          | Paso (playbook §4)       |   h |
| -- | ---------------------------------------------------------------------------------------------------------- | ----------------------- | --: |
| T1 | Adenda S1 con `admin-service`: `POST /api/llm/golden-sets` y `…/entries`; congelar los campos aún inexistentes | Contrato y amenaza      |   3 |
| T2 | Caso de uso «crear golden set»: validar `rubricVersion`/`language`, persistir, devolver `id` + header `Location` | Caso de uso           |   4 |
| T3 | Caso de uso «cargar entrada»: validar `transcript` (arreglo JSON, **no** instrucciones) y `referenceScores` (5×0–100) | Caso de uso        |   5 |
| T4 | Autorización: servicio de confianza + scope `llm.golden-set.manage` + usuario delegado; *ownership* de cohorte (`courses-service`) → `403` | Seguridad y resiliencia | 5 |
| T5 | Idempotencia: `Idempotency-Key` (UUID) obligatoria; el reintento devuelve el mismo recurso sin duplicar        | Seguridad y resiliencia |   4 |
| T6 | Auditoría de cada escritura y `400` *Problem Details* ante `referenceScores` incompleto o fuera de rango        | Observabilidad          |   3 |
|    | **Total**                                                                                                     |                         | **24** |

---

# LLM-S01-H06 — Consulta del golden set que sobrevive al reinicio

| | |
|---|---|
| **Épica** | EP-03 · Golden set y referencia humana |
| **Pareja** | P5 |
| **Dependencias** | H04 |
| **Estimación (plan)** | 14 h |
| **Tipo** | **Historia de usuario** (HU de valor) · **candidata a historia canónica** |
| **Requisito** | RF-IA-30 a 36 (golden set y calibración) |
| **Referente de producto** | *(a nombrar en Sprint 0)* |

## Descripción (Como / Quiero / Para)

- **Como:** docente autorizado de una cohorte
- **Quiero:** consultar mi golden set y sus entradas aunque el servicio se reinicie
- **Para:** confiar en que el dato de referencia que cargué persiste y puedo calibrar sobre él

## Notas / Observaciones

- **Reglas de negocio:** la consulta se hace por el Gateway con la misma autorización que la
  escritura. El listado es paginado (`page` desde 0, `size` entre 1 y 100) y viene ordenado por
  fecha de creación descendente. El detalle incluye las entradas del golden set. Una cohorte ajena
  **no aparece** en la respuesta.
- **Validaciones:** un `goldenSetId` inexistente devuelve `404` (no `500`); un `size` fuera del
  rango 1–100 devuelve `400`.
- **Datos obligatorios:** ninguno en el cuerpo (es `GET`); parámetros `rubricVersion` (opcional),
  `page`, `size`.
- **Performance:** los datos se leen de PostgreSQL con volumen persistente; tras
  `docker compose restart` la lectura devuelve exactamente lo cargado antes.
- **Seguridad:** sin scope `llm.golden-set.manage` y usuario delegado → `403`; el filtro por
  cohorte se aplica siempre, no es un parámetro opcional.
- **Accesibilidad:** no aplica en esta historia (la pantalla es H07).
- **Otros:** esta es la **historia candidata a canónica** del backlog: pequeña, entendida por
  todos y con un recorrido completo (autorización + lectura + persistencia). Su valor en puntos se
  fija en el Sprint 0 y queda como referencia de todas las demás.
- **Endpoints:**
  - `GET /api/llm/golden-sets?rubricVersion={v}&page={n}&size={s}` → lista de
    `{ id, version, rubricVersion, language, createdAt }`.
  - `GET /api/llm/golden-sets/{goldenSetId}` → lo anterior + `entries[] { id, transcript, referenceScores, createdAt }`.

## Criterios de Aceptación (CA)

- **CA1:** un golden set creado y con una entrada se puede consultar y devuelve esa entrada.
- **CA2:** después de `docker compose restart`, la misma consulta devuelve los mismos datos.
- **CA3:** el listado respeta `page`/`size` y el orden por creación descendente.
- **CA4 (negativo):** el golden set de otra cohorte no aparece en el listado y su detalle responde
  `404`.
- **CA5 (negativo):** `goldenSetId` con formato UUID pero inexistente → `404` *Problem Details*,
  nunca `500`.
- **CA6 (negativo):** `size=500` → `400`.

## BDD (mínimo 3 escenarios)

**Característica:** lectura del golden set con persistencia garantizada.

### Escenario 1 — La consulta sobrevive al reinicio (camino feliz)

- **Dado:** un docente que creó un golden set y le cargó una entrada
- **Cuando:** se ejecuta `docker compose restart` del servicio y su base
- **Y:** el docente vuelve a consultar el golden set por el Gateway
- **Entonces:** la respuesta `200` contiene el golden set y la entrada tal como se cargaron

### Escenario 2 — Aislamiento entre cohortes

- **Dado:** dos golden sets, uno de la cohorte del docente y otro de una cohorte ajena
- **Cuando:** el docente pide el listado
- **Entonces:** solo aparece el golden set de su cohorte
- **Y:** pedir el detalle del ajeno responde `404`

### Escenario 3 — Golden set inexistente

- **Dado:** un `goldenSetId` con formato válido que no corresponde a ningún golden set
- **Cuando:** el docente consulta su detalle
- **Entonces:** el sistema responde `404` con *Problem Details*, no `500`

### Escenario 4 — Paginación fuera de rango

- **Dado:** el endpoint de listado con `size` limitado a 1–100
- **Cuando:** el cliente pide `size=500`
- **Entonces:** el sistema responde `400` y no devuelve datos

## Prototipo

- **Capturas:** boceto del listado de golden sets (versión, rúbrica, idioma, fecha) y del detalle
  con las entradas y sus cinco puntajes.
- **Mock API / Swagger:** [adenda S1 del golden set](../contracts/llm-service-v1-s1-golden-set-adenda.md).

## Estimación / Prioridad

| Puntos (Fibonacci) | Prioridad (MoSCoW) |
|---|---|
| *(historia canónica — se estima primera en Sprint 0)* | Must |

## Dependencias / Impactos

- **Servicios involucrados:** Gateway, PostgreSQL, `courses-service` (filtro por cohorte).
- **Módulos afectados:** `api`, `application`, `infrastructure`, `security`.
- **Otros equipos / aprobaciones:** contrato de lectura acordado con `admin-service` (adenda S1).
- **Impacto en datos / migraciones:** solo lectura; exige que el volumen de PostgreSQL de H02
  persista.
- **Riesgos y mitigación:** si el volumen no persiste, la demo falla; se verifica con la prueba de
  reinicio de H09.

## Tareas

> Pasos técnicos del equipo (P5). Se cargan en Taiga como tareas hijas de la HU. Horas orientativas.

| #  | Tarea                                                                                                          | Paso (playbook §4)       |   h |
| -- | ---------------------------------------------------------------------------------------------------------- | ----------------------- | --: |
| T1 | Caso de uso «listar»: paginado (`page` desde 0, `size` 1–100), orden por creación descendente, filtro por cohorte siempre aplicado | Caso de uso    |   4 |
| T2 | Caso de uso «detalle»: golden set + `entries[]`; cohorte ajena → `404`; `goldenSetId` inexistente → `404`, nunca `500` | Caso de uso       |   4 |
| T3 | Autorización de lectura (mismo scope y usuario delegado que la escritura); `size=500` → `400`                   | Seguridad y resiliencia |   3 |
| T4 | Prueba de integración de persistencia: `docker compose restart` y la consulta devuelve exactamente lo cargado  | Prueba E2E              |   3 |
|    | **Total**                                                                                                     |                         | **14** |

---

# LLM-S01-H07 — Pantalla docente mínima del golden set

| | |
|---|---|
| **Épica** | EP-03 · Golden set y referencia humana |
| **Pareja** | P5 |
| **Dependencias** | H05, H06 |
| **Estimación (plan)** | 24 h |
| **Tipo** | **Historia de usuario** (HU de valor) |
| **Requisito** | RF-IA-30 a 36 (golden set y calibración) |
| **Referente de producto** | *(a nombrar en Sprint 0)* |

## Descripción (Como / Quiero / Para)

- **Como:** docente
- **Quiero:** una pantalla mínima para el alta, la carga y la consulta del golden set
- **Para:** trabajar la referencia de mi curso sin depender de Swagger ni de llamadas manuales

## Notas / Observaciones

- **Reglas de negocio:** la pantalla (aplicación Angular `llm-workbench`) hace el alta, la carga
  de entradas y la consulta **siempre a través del Gateway**, nunca contra el servicio directo.
  Muestra la autorización vigente (quién está operando).
- **Validaciones:** la transcripción se valida como arreglo JSON no vacío antes de enviarse, con
  el aviso visible de que **no se interpreta como instrucciones**; los cinco puntajes aceptan
  0–100.
- **Datos obligatorios:** transcripción y los cinco puntajes de referencia para cargar una
  entrada.
- **Performance:** estados de carga (`aria-live`) mientras se resuelven las llamadas.
- **Seguridad:** si el backend responde `403`, la pantalla muestra ese estado de forma explícita,
  no un error genérico.
- **Accesibilidad (WCAG AA):** etiquetas asociadas a cada campo, `fieldset`/`legend` para el grupo
  de puntajes, mensajes de error con `role="alert"`, foco manejado, operable por teclado y con
  lector de pantalla.
- **Otros:** si el backend no está disponible, la pantalla muestra «no se pudo contactar al
  backend», sin romperse.

## Criterios de Aceptación (CA)

- **CA1:** desde la pantalla, el docente crea un golden set, carga una entrada y la ve en el
  detalle, sin usar Swagger.
- **CA2:** todas las llamadas salen hacia el Gateway (`/api/llm/**`), no al servicio directo.
- **CA3:** hay estados visibles de carga y de error en cada operación.
- **CA4 (negativo):** con el backend caído, la pantalla muestra un aviso claro y sigue navegable.
- **CA5 (negativo):** una transcripción que no es JSON válido muestra un mensaje de validación y
  **no** se envía.
- **CA6 (negativo):** una respuesta `403` del backend se refleja como «no autorizado» en la
  interfaz.

## BDD (mínimo 3 escenarios)

**Característica:** interfaz docente mínima del golden set.

### Escenario 1 — Recorrido completo desde la pantalla (camino feliz)

- **Dado:** un docente autorizado con la pantalla abierta y el backend disponible
- **Cuando:** crea un golden set, carga una entrada con transcripción y puntajes, y abre el detalle
- **Entonces:** la entrada aparece listada con sus cinco puntajes y sus pesos

### Escenario 2 — Backend no disponible

- **Dado:** el `llm-service` detenido
- **Cuando:** el docente abre el listado de golden sets
- **Entonces:** la pantalla muestra «no se pudo contactar al backend» y no queda en blanco ni con
  un error sin contexto

### Escenario 3 — Transcripción inválida

- **Dado:** el formulario de carga de entrada
- **Cuando:** el docente pega un texto que no es un arreglo JSON y confirma
- **Entonces:** la pantalla muestra un mensaje de validación y no envía la request

### Escenario 4 — Operación no autorizada

- **Dado:** un docente cuyo backend responde `403` a la operación
- **Cuando:** intenta crear un golden set
- **Entonces:** la pantalla muestra el estado «no autorizado» con el detalle recibido

## Prototipo

- **Capturas / Wireframes:** bocetos de baja fidelidad de las tres vistas (listado, alta y detalle+carga) con matriz de trazabilidad BDD en [wireframes-h07-golden-set.md](../prototipos/wireframes-h07-golden-set.md); demo interactiva funcional en [`Demos/Golden Set`](../../Demos/Golden%20Set/README.md).
- **Storybook:** *(pendiente)* — componentes de listado, formulario y detalle.
- **Mock API / Swagger:** consume los endpoints de H05/H06 por el Gateway.

## Estimación / Prioridad

| Puntos (Fibonacci) | Prioridad (MoSCoW) |
|---|---|
| *(a asignar en Sprint 0)* | Must (la demo de S1 la necesita) |

## Dependencias / Impactos

- **Servicios involucrados:** Gateway, `llm-service` (H05/H06).
- **Módulos afectados:** `llm-workbench` (frontend Angular).
- **Otros equipos / aprobaciones:** frontend compartido / diseño de la plataforma (D01) para
  encuadre visual y autenticación.
- **Impacto en datos / migraciones:** ninguno.
- **Riesgos y mitigación:** si el frontend compartido no está definido, se entrega como app
  Angular autónoma que apunta al Gateway y se integra después.

## Tareas

> Pasos técnicos del equipo (P5), sobre la app Angular `llm-workbench`. Se cargan en Taiga como tareas hijas de la HU. Horas orientativas.

| #  | Tarea                                                                                                          | Foco                    |   h |
| -- | ---------------------------------------------------------------------------------------------------------- | ----------------------- | --: |
| T1 | Esqueleto de `llm-workbench`: ruteo, cliente HTTP y config para llamar **siempre** al Gateway (`/api/llm/**`)  | Base                    |   4 |
| T2 | Vista «listado» de golden sets (versión, rúbrica, idioma, fecha) con estados de carga y error                  | Consulta                |   4 |
| T3 | Vista «alta» de golden set + panel con la autorización vigente (quién está operando)                           | Alta                    |   3 |
| T4 | Vista «detalle + carga»: `fieldset`/`legend` de los cinco puntajes (0–100) y validación de `transcript` como JSON, con el aviso «no se interpreta como instrucciones» | Carga | 6 |
| T5 | Manejo de estados: backend caído → «no se pudo contactar al backend»; respuesta `403` → «no autorizado» explícito | Robustez             |   3 |
| T6 | Accesibilidad WCAG AA (labels, `role="alert"`, foco, teclado, `aria-live`) + pruebas de componente             | Accesibilidad y pruebas |   4 |
|    | **Total**                                                                                                     |                         | **24** |

---

# LLM-S01-H08 — Contrato OpenAPI y mock del golden set publicados

| | |
|---|---|
| **Épica** | EP-01 · Plataforma, contratos e integración |
| **Pareja** | P1 |
| **Dependencias** | H03 |
| **Estimación (plan)** | 10 h |
| **Tipo** | Tarea / habilitador técnico |
| **Requisito** | contratos v1; RF-NFR-01 (orientativo) |
| **Referente de producto** | *(a nombrar en Sprint 0)* |

## Descripción (Como / Quiero / Para)

- **Como:** equipo de integración (y `admin-service` como consumidor)
- **Quiero:** el contrato OpenAPI y un mock del golden set publicados
- **Para:** que `admin-service` avance su integración sin esperar al servicio real

## Notas / Observaciones

- **Reglas de negocio:** el contrato en el repositorio (`docs/contracts/`) describe **solo** las
  operaciones que existen; la [adenda S1](../contracts/llm-service-v1-s1-golden-set-adenda.md) se
  fusiona en el OpenAPI cuando `admin-service` aprueba el cambio.
- **Validaciones:** el OpenAPI valida contra su esquema; el mock responde con cuerpos conformes al
  contrato.
- **Datos obligatorios:** archivo OpenAPI, adenda, comando de una línea para levantar el mock,
  ubicación documentada.
- **Performance:** no aplica.
- **Seguridad:** el mock no expone datos reales.
- **Accesibilidad:** no aplica.
- **Otros:** cualquier campo que hoy no exista se **congela** con `admin-service` antes de
  publicarlo — no se agrega al contrato «por las dudas».

## Criterios de Aceptación (CA)

- **CA1:** el OpenAPI publicado valida y describe únicamente las operaciones implementadas
  (crear golden set, crear entrada, listar, obtener detalle).
- **CA2:** el mock se levanta con un comando documentado y responde según el contrato.
- **CA3:** la adenda S1 está revisada con `admin-service` y registrada.
- **CA4 (negativo):** un PR que agrega al contrato una operación no implementada se rechaza.
- **CA5 (negativo):** un campo nuevo sin acuerdo con `admin-service` bloquea la publicación.

## BDD (mínimo 3 escenarios)

**Característica:** contrato y mock del golden set para consumidores.

### Escenario 1 — admin-service integra contra el mock

- **Dado:** el contrato OpenAPI y el mock publicados
- **Cuando:** `admin-service` levanta el mock con el comando documentado y prueba su integración
- **Entonces:** el mock responde conforme al contrato y `admin-service` avanza sin el servicio real

### Escenario 2 — Operación inexistente en el contrato

- **Dado:** el acuerdo de publicar solo operaciones implementadas
- **Cuando:** un PR agrega al OpenAPI una operación que el servicio no expone
- **Entonces:** la revisión rechaza el PR

### Escenario 3 — Campo no acordado

- **Dado:** un campo de respuesta que hoy no existe en el servicio
- **Cuando:** alguien intenta incluirlo en el contrato publicado
- **Entonces:** la publicación se bloquea hasta congelar el campo con `admin-service`

## Prototipo

- **Capturas:** no aplica.
- **Mock API / Swagger:** [`contracts/llm-service-v1.openapi.yaml`](../contracts/llm-service-v1.openapi.yaml)
  + [adenda S1](../contracts/llm-service-v1-s1-golden-set-adenda.md).

## Estimación / Prioridad

| Puntos (Fibonacci) | Prioridad (MoSCoW) |
|---|---|
| *(a asignar en Sprint 0)* | Must |

## Dependencias / Impactos

- **Servicios involucrados:** `admin-service` (consumidor).
- **Módulos afectados:** `docs/contracts/`.
- **Otros equipos / aprobaciones:** `admin-service` aprueba la adenda y los campos.
- **Impacto en datos / migraciones:** ninguno.
- **Riesgos y mitigación:** divergencia contrato ↔ implementación; se cubre con pruebas de
  contrato en H09.

## Tareas

> Pasos técnicos del equipo (P1). Se cargan en Taiga como tareas de la tarea de sprint. Horas orientativas.

| #  | Tarea                                                                                                          | Foco                    |   h |
| -- | ---------------------------------------------------------------------------------------------------------- | ----------------------- | --: |
| T1 | Completar `llm-service-v1.openapi.yaml` con **solo** las cuatro operaciones existentes (crear golden set, crear entrada, listar, detalle) y validarlo contra su esquema | Contrato |   4 |
| T2 | Revisar y registrar la adenda S1 con `admin-service`; congelar cualquier campo no acordado antes de publicar   | Acuerdo                 |   2 |
| T3 | Mock levantable con un comando documentado (p. ej. Prism), con cuerpos conformes al contrato                   | Mock                    |   2 |
| T4 | Check en CI / revisión que rechaza un PR que agregue una operación no implementada o un campo no acordado       | Verificación            |   2 |
|    | **Total**                                                                                                     |                         | **10** |

---

# LLM-S01-H09 — Suite de pruebas y guía de demo de S1

| | |
|---|---|
| **Épica** | EP-01 · Plataforma, contratos e integración |
| **Pareja** | todas (una persona por pareja) |
| **Dependencias** | H04, H05, H06, H07 |
| **Estimación (plan)** | 18 h |
| **Tipo** | Tarea / habilitador técnico |
| **Requisito** | [24 · Convención de cobertura](../24-convenciones-cobertura.md); [25 · Matriz de pruebas de infraestructura](../25-matriz-pruebas-infraestructura.md) |
| **Referente de producto** | *(a nombrar en Sprint 0)* |

## Descripción (Como / Quiero / Para)

- **Como:** equipo de desarrollo
- **Quiero:** la suite de pruebas y la guía de demo de S1
- **Para:** validar la Review con evidencia reproducible y no con relato

## Notas / Observaciones

- **Reglas de negocio:** la suite cubre unitarias de reglas de dominio, integración de
  persistencia y migración con Testcontainers/Flyway, contrato del Gateway con WireMock y la
  prueba de **reinicio de Compose**. La cobertura cumple el umbral de [24](../24-convenciones-cobertura.md)
  (95 % back/front).
- **Validaciones:** los **escenarios BDD de aceptación** de H05–H07 (camino feliz + negativos)
  quedan ejecutados con evidencia.
- **Datos obligatorios:** guía de demo paso a paso: acceso autorizado → alta → carga → consulta
  tras reinicio.
- **Performance:** la suite corre en CI en un tiempo acotado.
- **Seguridad:** se prueban los casos de autorización (`401`/`403`) y de idempotencia.
- **Accesibilidad:** el front incluye pruebas de componente; las verificaciones de accesibilidad
  de H07 se listan en la guía.
- **Otros:** la guía de demo es ejecutable por cualquier integrante en un ambiente integrado, no
  en una máquina local aislada.

## Criterios de Aceptación (CA)

- **CA1:** la suite completa corre en verde en CI.
- **CA2:** la cobertura back y front alcanza el umbral de [24](../24-convenciones-cobertura.md).
- **CA3:** existe una prueba automatizada que reinicia Compose y verifica que el golden set
  persiste.
- **CA4:** la guía de demo se puede seguir paso a paso y termina en la consulta tras reinicio.
- **CA5 (negativo):** si la cobertura baja del umbral, CI falla el PR.
- **CA6 (negativo):** si la prueba de reinicio detecta pérdida de datos, falla y bloquea la
  Review.

## BDD (mínimo 3 escenarios)

**Característica:** evidencia reproducible del incremento de S1.

### Escenario 1 — La suite valida el incremento (camino feliz)

- **Dado:** el código de S1 integrado
- **Cuando:** CI ejecuta la suite completa
- **Entonces:** todas las pruebas pasan y la cobertura queda por encima del umbral de [24](../24-convenciones-cobertura.md)

### Escenario 2 — Cobertura insuficiente

- **Dado:** un PR que agrega código sin pruebas
- **Cuando:** CI mide la cobertura
- **Entonces:** la cobertura cae bajo el umbral y CI marca el PR como fallido

### Escenario 3 — Pérdida de datos en el reinicio

- **Dado:** la prueba automatizada de reinicio de Compose
- **Cuando:** tras el reinicio el golden set cargado antes no está
- **Entonces:** la prueba falla y la Review no puede aceptar el incremento

## Prototipo

- **Capturas:** no aplica.
- **Mock API / Swagger:** WireMock del Gateway; Testcontainers de PostgreSQL.

## Estimación / Prioridad

| Puntos (Fibonacci) | Prioridad (MoSCoW) |
|---|---|
| *(a asignar en Sprint 0)* | Must |

## Dependencias / Impactos

- **Servicios involucrados:** CI, PostgreSQL (Testcontainers), Gateway (WireMock).
- **Módulos afectados:** `src/test` del servicio y del `llm-workbench`; documentación de demo.
- **Otros equipos / aprobaciones:** ninguna externa.
- **Impacto en datos / migraciones:** las pruebas usan bases efímeras; no tocan datos reales.
- **Riesgos y mitigación:** una demo local que no representa el ambiente integrado; se mitiga
  exigiendo que la guía corra en el ambiente integrado (DoD por incremento, [23 · §9.2](../23-plan-construccion-producto-llm.md)).

## Tareas

> Pasos técnicos del equipo (una persona por pareja). Se cargan en Taiga como tareas de la tarea de sprint. Horas orientativas.

| #  | Tarea                                                                                                          | Tipo de prueba ([playbook §6](<../../Plan de ejecucion/06-playbook-de-construccion.md>)) |   h |
| -- | ---------------------------------------------------------------------------------------------------------- | ----------------------- | --: |
| T1 | Unitarias de reglas de dominio (cálculo de rúbrica, invariantes, validaciones) sin Spring ni red              | Unidad                  |   4 |
| T2 | Integración de persistencia y migración con Testcontainers / Flyway desde base vacía                          | Integración             |   3 |
| T3 | Pruebas de contrato del Gateway con WireMock (headers, `401`/`403`, idempotencia)                              | Contrato / Seguridad    |   3 |
| T4 | Prueba automatizada que reinicia Compose y verifica que el golden set persiste                                 | Resiliencia             |   3 |
| T5 | Ejecutar los escenarios BDD de aceptación de H05–H07 (camino feliz + negativos) con evidencia enlazada         | E2E                     |   3 |
| T6 | Guía de demo paso a paso (acceso autorizado → alta → carga → consulta tras reinicio), ejecutable en ambiente integrado; gate de cobertura de [24](../24-convenciones-cobertura.md) en CI | E2E / demo | 2 |
|    | **Total**                                                                                                     |                         | **18** |
